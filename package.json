{
  "name": "program-test",
  "scripts": {
    "test": "cd example/counter && elm-test --compiler `which lamdera`",
    "test2": "cd example/counter && elm-test --compiler ../../node_modules/.bin/lamdera"
  },
  "devDependencies": {
    "elm-format": "^0.8.8",
    "elm-test": "^0.19.1-revision16",
    "lamdera": "^0.19.1-1.3.2"
  }
}
